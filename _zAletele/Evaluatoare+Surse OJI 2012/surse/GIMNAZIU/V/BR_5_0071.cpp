#include <fstream>
#include<iostream>
/////problema 1////
using namespace std;

{
ifstream fin('chibrituri.in');
ofstream fout('chibrituri.out');
long n,m,hh,mm;
int main()
{
else(n,m==hh,mm);
{
f>>min(=m,n);
if(m:n==11:11);
g>>max(==m,n);
if(m:n==18:58);
 fin.close;
 fout.close;
 return 0;
}
