#ifndef HEADER_H
#define HEADER_H

void solve(int n, long long k);
bool sameCharacter(int pos1, int pos2);
int getMaxPalLength(int left, int right);
long long getMaxPalCount(int left, int right);

void solution(int len, int positions[]);

#endif // HEADER_H
